## 업무계획
### 0511
#### 오늘 한것 
교재를 바탕으로 온라인샵 구현해보기(미완)

#### 오늘 발생한 문제점

*규호 :   
1. 결제페이지에서 팝업창이 해결이 안됨  
2. Reverse 뭐라고 뜨면서 값이 전달되지 못함
3. 버킷에 이미지 저장을 하려 하자 터미널에 권한이 없다고 나옴

*재중 : 
1. 게시글 작성 후 메인페이지에 "The'image' attribute has no file associated with it" 오류로 진행되지 않음
2. cart 에서 결제 페이지로 넘어가지 않음

#### 문제점에 대한 해결 방안

*규호 :   
1. 카카오나 네이버는 결제가 안되지만 다른 은행들은 결제가 가능 이에대해서는 좀더 찾아봐야할 듯
2. url부분에서 config의 urls.py에 패스순서를 바꿔주니 해결됨 
3. 버킷 권한에서 정책부분을 작성하고 ACL 부분도 활성화 시켜서 해결

*재중 : 
1. DB에 image가 없는 정보가 들어가면 오류가 뜨는 것으로 확인. image 미첨부시 저장되지 않도록 수정 예정

#### 앞으로 해야할것
onlineshop에서 우리프로젝트에서 사용할 부분 결정  
기존 airbnb 템플릿, css, 적용시켜보기  
관리자 말고도 게시글 생성가능하게 하기  
로그인페이지 따로 띄어서 하는거 알아보기  

+ 시간되면 추가로 사용할 api 찾아보기  




### 0512
#### 오늘 한것
교재를 바탕으로 온라인샵 구현해보기(완)

#### 오늘 발생한 문제점

*규호 :   
1. pdf가 출력이 안됨 (해결 아직못함)
2. 
*재중 : 
1. order 까지는 진행이 됐지만 cart 에서 결제로 넘어가지 않는 문제점 지속.
2. admin 에서 작성 및 수정이 기본으로 설정 됐는데 사용자도 등록이 가능하게 새로운 경로 및 templates 작성에서 막힘

#### 문제점에 대한 해결 방안

1. 


#### 앞으로 해야할것
* 기존에 해결해야할 것 목록  
onlineshop에서 우리프로젝트에서 사용할 부분 결정  
기존 airbnb 템플릿, css, 적용시켜보기  
관리자 말고도 게시글 생성가능하게 하기  
로그인페이지 따로 띄어서 하는거 알아보기  
+ 시간되면 추가로 사용할 api 찾아보기  

* 오늘 추가된 앞으로 해야할 것 목록
각 앱마다 DB에 넣어야 할 모델들 정하고 어떻게 수정할지 생각해보기  




### 0513
#### 오늘 한것
로그인페이지 부분 수정하는법 찾아봄
게시글 생성가능하게 함
#### 오늘 발생한 문제점
*규호 :
1. 카카오 로그인 오류 발생


*재중 : 
1. admin 의 product를 다른 html에서 띄우기 위해서 forms 생성 및 views에 create_product를 넣었지만 오류 발생
2. create_product 생성하여 정상적으로 DB에 들어가는데 image 부분이 누락됨. S3와 DB 2군데 모두 들어가지 않음
3. admin product 에서 image 파일이 없이 업로드시 메인페이지 error
4. django.core.exceptions.ImproperlyConfigured: mysqlclient 1.4.0 or newer is required; you have 1.0.2.

#### 문제점에 대한 해결 방안
*규호 :
1. KOE006은 redirect 에러기 때문에 이를 확인하기 위한 크롬 익스텐션을 설치해서 확인후 카카오 redirect 부분 수정

*재중 : 
1. form에 자동 생성되는 date 내역이 있어 오류. 해당 항목 삭제로 해결
2. models 에서 image 부분에 blank=True(필수 입력 사항 아님) 을 없애면 image가 없을 시 작성이 안돼서 오류 뜰 일이 없음.
3. error 내용에 따르면 버전 문제인데 동일한 조건이지만 이번에만 뜬 내용이라 설정 파일에 들어가서 pass 및 주석 처리로 해결

#### 앞으로 해야할것
* 기존에 해결해야할 것 목록
기존 airbnb 템플릿, css, 적용시켜보기    
추가로 사용할 api 찾아보기  
* 오늘 추가된 앞으로 해야할 것 목록


### 0516
#### 오늘 한것
airbnb 템플릿 따온걸로 allauth 템플릿 수정
#### 오늘 발생한 문제점

*재중 : 게시글 생성으로 작성한 내용이 DB로 들어가지 않음. 기존에는 image만 안들어갔는데 이젠 다 안들어감

#### 문제점에 대한 해결 방안
#### 앞으로 해야할것
* 기존에 해결해야할 것 목록
기존 airbnb 템플릿, css, 적용시켜보기    
추가로 사용할 api 찾아보기  
* 오늘 추가된 앞으로 해야할 것 목록


### 0517
#### 오늘 한것
회원가입시 이메일 인증 구현 시도
#### 오늘 발생한 문제점
*규호 : 이메일 인증을 위해 이메일을 필수 값으로 하면 컴퓨터 연결이 거부됨
*재중 : 
#### 문제점에 대한 해결 방안
settings에 이메일 인증을 백으로 돌리는 코드를 추가로 해결 그러나 추후 이메일관련코드 수정이 필요
#### 앞으로 해야할것
* 기존에 해결해야할 것 목록
기존 airbnb 템플릿, css, 적용시켜보기    
추가로 사용할 api 찾아보기 
* 오늘 추가된 앞으로 해야할 것 목록
이메일 관련 서비스 가입 및 이메일을 보내줄 계정 만들기



### 0518
#### 오늘 한것
회원가입시 추가항목 설정, 회원정보 수정 구현
게시글 사진 다중 업로드 및 리스트에 여러 사진들 나오게 구현
#### 오늘 발생한 문제점
*규호 :   
*재중 : 게시글의 다중사진 업로드 후 해당 사진을 형식에 맞게 슬라이드 형식으로 구현하고자 했으나 실패
#### 문제점에 대한 해결 방안
#### 앞으로 해야할것
* 기존에 해결해야할 것 목록
* 오늘 추가된 앞으로 해야할 것 목록

### 0519
#### 오늘 한것
프로필 페이지에서 비밀번호 변경, 회원탈퇴 구현, 회원가입 이메일 인증
게시글에 옵션 여러가지 넣음.
html 에 form 및 기능테스트
#### 오늘 발생한 문제점
*규호 : redirect로 페이지 이동할때 제대로 이동이 안되었음,  이메일 인증시 smtp가 제대로 반영이 안됨  
*재중 : 게시글 수정 페이지로 이동시 오류
#### 문제점에 대한 해결 방안
규호 :
render로 html 페이지 이동으로 해결, 
이메일 인증 비밀번호로 구글계정 비번이 아닌 메일 앱 비밀번호를 넣어서 해결
#### 앞으로 해야할것
* 기존에 해결해야할 것 목록
* 오늘 추가된 앞으로 해야할 것 목록
예약페이지 제대로 구현

### 0520
#### 오늘 한것
검색기능 구현  
댓글 작성, 삭제 기능 구현
#### 오늘 발생한 문제점
*규호 : css적인 문제로 아이콘이 인식이 안됨  
*재중 : 댓글 delete 기능은 수행이 되지만 redirect가 되지 않음.
#### 문제점에 대한 해결 방안
아이콘이라서 그냥 넘기기로함 나중에 해결방안을 찾아야할듯  
#### 앞으로 해야할것
* 기존에 해결해야할 것 목록
* 오늘 추가된 앞으로 해야할 것 목록

### 0523
#### 오늘 한것
주문조회 기능 구현 시도, 내가 등록한 상품 조회 기능 구현 시도  
#### 오늘 발생한 문제점
*규호 : 주문조회할때 user세션과 내가 필터로 비교해야하는 필드값이 비교가 안됨  
*재중 : 게시글 경로 설정 문제. namespace 활용 능력 부족
#### 문제점에 대한 해결 방안  
세션값이 있는지 확인해보고 없다면 로그인 세션을 구현하거나 강사님에게 조언 구해보기
#### 앞으로 해야할것
* 기존에 해결해야할 것 목록
* 오늘 추가된 앞으로 해야할 것 목록


### 0524
#### 오늘 한것
주문조회 기능 구현 시도, 내가 등록한 상품 조회 기능 구현 시도  
주소 api 및 지도 api 활용 주소지 및 지도 구현
#### 오늘 발생한 문제점
*규호 : 세션값이 없어서 필터로 비교가 안됨, 모델에서 auth_user_model을 foreign key의 값으로 넣어줄때 값을 못 받아옴,  
orderitem이라는 테이블에 원하는 값이 안담김  
*재중 : 지도 api에서 현재 위치를 geolocation 에서 가져오지 못함. 학원 내 컴퓨터에서도 gps 기능 작동하지 않음.
#### 문제점에 대한 해결 방안
강사님의 도움으로 세션과의 비교가 아닌 그냥 request유저로 비교 가능한것을 알게됨  
auth_user_model을 import 시키는 방법이 틀렸었음
기존에는 settings.auth_user_model 를 썼지만 auth_user_model로 하니 해결됨
학원 컴퓨터 말고 배포 시키고 집에서 해 봐야 할 듯
#### 앞으로 해야할것
* 기존에 해결해야할 것 목록
* 오늘 추가된 앞으로 해야할 것 목록


### 0525
#### 오늘 한것
게시글 및 댓글 기능 보완, detail, list 출력 세부항목 추가  
order 폼의 필드값 유저이름과 상품이름을 선택하지않고 db에 저장하는것 구현 시도  
#### 오늘 발생한 문제점
*규호 : user는 request user로 해결이 되는데 상품이름을 저장하지 못하고 있음  
*재중 : select options 값이 selected 되지 않음. DB로 값이 넘어가지 않음.
#### 문제점에 대한 해결 방안

#### 앞으로 해야할것
* 기존에 해결해야할 것 목록
* 오늘 추가된 앞으로 해야할 것 목록


### 0526
#### 오늘 한것
게시글, 댓글 작성 수정 삭제 및 이전화면 이동과 세부항목 수정. 기본기능 완료  
order라는 테이블에 리퀘스트 유저로 유저값을 불러와서 담음

#### 오늘 발생한 문제점
*규호 : 아직 상품이름을 가져올 수 있는 방법을 찾지 못함  
*재중 : models 의 product 안에 category(외래키) 에 대한 choices 를 적용 시켜봤는데 오류
ValueError: too many values to unpack (expected 2)
#### 문제점에 대한 해결 방안
cart view에 있는 함수내에서 시도해봐야할 듯함
#### 앞으로 해야할것
* 기존에 해결해야할 것 목록
* 오늘 추가된 앞으로 해야할 것 목록


### 0527
#### 오늘 한것
ordercreate 함수에서 cart 내역을 불러오는 부분이 있어서 이부분에서 카트에 담긴 내역을 불러올 수 있어서  
그 부분에서 상품이름을 가져와서 order테이블에 저장함
페이징 구현 및 템플릿 추가 적용

#### 오늘 발생한 문제점
*규호 : 그냥 페이징은 괜찮은데 검색결과 페이징이 데이터를 못 불러오는 일이 생김    
*재중 : 
#### 문제점에 대한 해결 방안
페이징 부분 view를 수정하거나 다른 방법으로 불러오는법 찾아보기
#### 앞으로 해야할것
* 기존에 해결해야할 것 목록
* 오늘 추가된 앞으로 해야할 것 목록

### 0530
#### 오늘 한것
페이징 완료 이용을 원하는 날짜로 검색 시도

#### 오늘 발생한 문제점
*규호 : calender를 html에서 나타내는게 잘 안됨  
*재중 : 
#### 문제점에 대한 해결 방안
캘린더를 불러오는 방법은 여러개가 있으니 다른 방법들 시도 부트스트랩 캘린더는 실패 
#### 앞으로 해야할것
* 기존에 해결해야할 것 목록
* 오늘 추가된 앞으로 해야할 것 목록

### 0531
#### 오늘 한것
캘린더 검색 기능 구현, 거래후 재고량 바뀌는것 구현

#### 오늘 발생한 문제점
*규호 :  
*재중 : 
#### 문제점에 대한 해결 방안
#### 앞으로 해야할것
* 기존에 해결해야할 것 목록
* 오늘 추가된 앞으로 해야할 것 목록

### 0602
#### 오늘 한것
젠킨스 연동, 젠킨스 배포 시도

#### 오늘 발생한 문제점
*규호 : 웹훅연결이 안됨, 젠킨스에서 repository연동이 안됨
*재중 : 
#### 문제점에 대한 해결 방안
엑세스 토큰을 발급발을때 훅 부분을 다시 체크해서 재등록   
우분투에 git이 없어서 발생한 문제라 git을 install해서 해결 
#### 앞으로 해야할것
* 기존에 해결해야할 것 목록
* 오늘 추가된 앞으로 해야할 것 목록  


### 0603
#### 오늘 한것
ec2에 젠킨스 설치, 
#### 오늘 발생한 문제점
*규호 : 
*재중 : 
#### 문제점에 대한 해결 방안
#### 앞으로 해야할것
* 기존에 해결해야할 것 목록
* 오늘 추가된 앞으로 해야할 것 목록

